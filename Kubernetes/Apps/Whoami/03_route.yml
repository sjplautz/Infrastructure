apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: whoami-route
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - http:
      paths:
      - path: /whoami
        backend:
          serviceName: whoami-svc
          servicePort: 80