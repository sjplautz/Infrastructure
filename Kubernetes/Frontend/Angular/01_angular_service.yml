apiVersion: v1
kind: Service
metadata: 
  name: angular-service
  labels:
    app: web-portfolio
    tier: front-end
spec:
  type: NodePort
  selector:
    app: web-portfolio
    tier: front-end
  ports:
    - name: http
      protocol: TCP
      nodePort: 32700
      port: 443
      targetPort: 80